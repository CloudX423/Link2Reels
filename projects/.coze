# Coze Project Configuration
[project]
requires = ["python-3.12"]
entrypoint = "app.py"
template = "flask-app"
version = "0.0.14"

[dev]
build = ["pip3 install -r requirements.txt"]
run = ["python3", "app.py"]

[deploy]
build = ["pip3 install -r requirements.txt"]
run = ["python3", "app.py"]
